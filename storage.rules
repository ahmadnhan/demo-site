rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /shared/{allPaths=**} {
      allow read: if true; // عام مؤقت
      allow write: if request.time < timestamp.date(2025, 12, 31); // مؤقت للتجربة
    }
  }
}
